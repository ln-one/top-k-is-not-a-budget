# 4 结论

现代 RAG 混合检索面对多样的查询和持续更新的语料，固定 L 难以适应排名变化，而精确返回固定 K 条结果所需的读取量也不稳定。DiBud 以读取预算为约束，保留未读贡献的上下界，返回预算内已确认的精确前缀，使结果数量随当前证据变化。五个查询集上的回放表明，选择性读取提高了同预算下的认证产出；跨语料快照的实验进一步显示，返回数量随语料变化，已交付结果始终与对应的完整 RRF 排序一致。
