固定深度融合在不同查询与语料快照上的相对效果可能变化，精确 Top-K 的完成成本也仍然不稳定。DiBud 直接约束顺序访问次数，由增量认证确定返回前缀。同预算回放中，DiBud 相比均衡访问增加了认证产出；留出查询上的预算校准在多个集合保留较高平均检索质量的同时，减少了访问量。这一取舍随集合变化：访问减少可能意味着返回结果减少，质量目标也不保证对每条查询成立。当前证据计量精确排名上的逻辑访问，不涉及在线检索时延。
